<!--Duplicate this markup in the Waits index view.-->

  <div class="container-fluid">
   <div class=row-fluid">
   <div class="span12"><h3><%= @location.name %></h3></div>
   </div>
   <div class="row-fluid"> 
   <div class="span12"><%= image_tag(@location.image_url) %></div>
   </div>
<div class="row-fluid">
<div class="span12"><%= link_to 'Current Wait', location_waits_path(@location), class: "btn btn-large btn-block btn-primary" %></div>
  </div>

<p><%= link_to 'See all locations', locations_path %></p>
